{
  "name": "dumpsock-docs",
  "version": "0.1.0",
  "description": "DumpSock docs — external (public, served at dumpsock.hartle.tech root) + internal (tailnet-only, served at dumpsock.hartle.tech/wiki). Both built by Caddy + product_docs Ansible role on the OVH VPS.",
  "private": true,
  "type": "module",
  "scripts": {
    "external:dev":     "vitepress dev external",
    "external:build":   "vitepress build external",
    "external:preview": "vitepress preview external",
    "internal:dev":     "vitepress dev internal --port 5175",
    "internal:build":   "vitepress build internal",
    "internal:preview": "vitepress preview internal --port 5175",
    "build":            "npm run external:build && npm run internal:build"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
