// External (public) docs theme — extends VitePress default theme + applies
// DumpSock brand tokens. Mascot, primary red (#E74C3C), sock-white surface,
// ink-black text. Slab-serif headings (IBM Plex Serif), Inter body,
// JetBrains Mono for code.
//
// Brand mockups: /Users/vz/Documents/mockups.zip (operator-supplied).
// Reference image 1 (brand spec) for the canonical palette + type.

import DefaultTheme from 'vitepress/theme'
import './style.css'

export default DefaultTheme
