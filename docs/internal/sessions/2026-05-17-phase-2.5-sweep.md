# 2026-05-17 — Phase 2.5 GUI sweep + nonnegotiable workflow retrofit

Session length: ~1 day of continuous operator-Claude pair work.

## What landed

Single combined commit `dfbe600` on `develop`, pushed to origin, tracked under GitHub umbrella issue **#27** on org project board #7 (status: In Progress).

**Validated (14 tasks):**

- Icon redesign → blue-bg (`#66D1FF`) variant adopted as canonical `assets/brand/dumpsock-icon.svg`. Full-bleed variant kept at `assets/brand/dumpsock-icon-fullbleed.svg`.
- **Chrome bar removal** — killed the 24-px `<header class="chrome">` strip; sidebar runs full window height with 42-px padding-top reserving macOS traffic-light space.
- **iPhone-row click fix** — dropped the bogus `sidebar of window 1` AppleScript; now opens Finder at `~` + toasts the user toward Locations.
- **Byte-based progress bar + files-remaining counter** — `BytesTotal`/`BytesPulled` added to `ProgressEvent`; engine sums totals + atomic-adds per pulled file. Frontend renders bytes (more honest than file count when sizes vary).
- **Password-protect ↔ Compress dependency** — UI grays out Password-protect when Compress is unchecked.
- **Browse iPhone tab** — `App.BrowseRemote` + `afc.Client.List`/`Stat`; `backup.Options.OnlyPaths` short-circuits Walk; selective backup.
- **Storage donut click-through** — `data-browse-to` on legend rows and segments.
- **Third-party app data browser** — `App.ListFileSharingApps` + `App.BrowseApp` via new `internal/afc/housearrest.go` wrapper (VendDocuments first, VendContainer fallback).
- **Folder selection + folder ops** — folder checkboxes; `ExpandRemoteSelection` flattens; per-row Delete works on folders via `RemoveAll`. Rename stubbed pending upstream.
- **RemoteRemove verification** — post-delete `Stat` re-check surfaces "iOS held onto this" honestly.
- **In-app confirm modal** — replaces `window.confirm()` which doesn't surface reliably in Wails webview.
- **Readable Delete button** — new `.btn-text-danger` (transparent bg, red text). Fixes the red-on-red invisible-label bug.
- **App picker filtering** — `BrowseUserApps` + `UIFileSharingEnabled` host-side filter (go-ios's `BrowseFileSharingApps` doesn't filter, despite the name).
- **AFC error humanisation** — codes 8/9/10/11/13/15/16/18/20 translated to English with actionable hints.
- **Greyed iOS-hardened rows** — entries that fail Stat (post-iOS-15 hardening) appear with 🔒, "hidden by iOS" size, disabled actions, plus a footer count.

**In-flight (not yet validated by operator):**

- Session restore via `.dumpsock-session.json` (task #5).
- Backups list + `.dumpsock.json` metadata + Move/Forget (task #6).
- Wring it dry + Compress + Password output (task #7) — operator hit "hung then said done, no zip"; fix in task #20 surfaces `package_error`/`zip_path`/`encrypted_path` + a toast with the artifact path. Awaiting re-test.
- Scope expansion for iCloud + full-device backup (task #11) — CLAUDE.md non-goals softened; scaffolds shipped; awaiting sign-off.
- AFC rename via go-ios PR or in-repo opcode wrapper (task #21) — decision pending.

## Workflow rules cemented this session

Operator handed down five **nonnegotiable** rules after I shipped 10 features without committing, testing, updating docs, or updating the project board:

1. Commit AFTER testing.
2. Update wiki / docs / landing on every state change.
3. TaskCreate BEFORE touching work.
4. Sync the GitHub project board.
5. Update memory + SESSION_RESUME after every delivery.

Saved as `feedback_nonnegotiables.md` in the project memory directory. Binding for all future sessions.

## Things we discovered

See the new docs:

- [go-ios v1.0.213 limitations](/internal/discoveries/) — `BrowseFileSharingApps` is misnamed, `house_arrest.New` hardcodes VendContainer, no MobileBackup2, no AFC Rename.
- [Wails webview gotchas](/internal/discoveries/) — `window.confirm()` doesn't surface; `.btn.danger` is a red-on-red trap.
- [iOS 15+ hardening cliffs](/internal/discoveries/) — `/PhotoData/Metadata`, `/PhotoData/Photos.sqlite`, most app sandboxes EPERM under AFC.

## Process notes

10 parallel research agents fired at the start of the session to map out the feasibility of items 6, 8, 10. Outputs synthesised into `docs/phase-6-roadmap.md` and inline implementation choices. Agents:

| Topic | Returned with |
|---|---|
| AFC accessible paths | Full path tree + R/W flags + version cliffs |
| UIFileSharingEnabled + house_arrest | Protocol summary + go-ios surface |
| DJI Fly browse capability map | Mechanism confirmed; honest capability matrix |
| RevealDeviceInFinder audit | Root cause + concrete fix proposed |
| Alt Finder iPhone routes | Reliability ranking; recommendation against AMDevice |
| iCloud Photos auth landscape | SRP-6a, 30-day trust tokens, Q1 2026 state |
| MobileBackup2 + go-ios | Zero support; ~3-5kLOC port effort |
| iOS backup format | Manifest.db schema, BackupKeyBag, encryption mechanism |
| iOS backup restore feasibility | Unfeasible for third-party tools on iOS 17+; drop |
| Plan synthesis | Phased roadmap → `docs/phase-6-roadmap.md` |

## Open questions for next session

- Per-task atomic commits vs sweep commits for the next round? Today we shipped one umbrella commit because the frontend files were too tangled for hunk surgery against an "implement in parallel" directive.
- Phase 6 unencrypted-first MVP or full encrypted MVP? Recommendation in the roadmap is unencrypted first.
- Phase 7 vendored fork of `chyroc/icloudgo` vs fresh port of `pyicloud_ipd`?
- When do Phase 6/7 features get a GUI tab vs CLI-only?
